SITE_URL=https://apt-portfolio.apt-account.workers.dev/
VITE_RECENT_WINDOW_DAYS=60

# Base URL for Playwright tests (override to prod when running against deployment)
PW_BASE_URL=http://localhost:5173

# Optional: demo/test credentials used by test pages (do NOT commit real secrets)
API_LOGIN_ID=
TRANSACTION_KEY=
